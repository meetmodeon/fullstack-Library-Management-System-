import { SortContentPipePipe } from './sort-content-pipe.pipe';

describe('SortContentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortContentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
