/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageRatingFeedBackResponse } from '../../models/page-rating-feed-back-response';

export interface GetRatingFeedBackByUserId$Params {
  bookId: number;
  page?: number;
  size?: number;
  sortBy?: string;
  direction?: string;
}

export function getRatingFeedBackByUserId(http: HttpClient, rootUrl: string, params: GetRatingFeedBackByUserId$Params, context?: HttpContext): Observable<StrictHttpResponse<PageRatingFeedBackResponse>> {
  const rb = new RequestBuilder(rootUrl, getRatingFeedBackByUserId.PATH, 'get');
  if (params) {
    rb.query('bookId', params.bookId, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('direction', params.direction, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageRatingFeedBackResponse>;
    })
  );
}

getRatingFeedBackByUserId.PATH = '/ratings/getAllBookRating';
